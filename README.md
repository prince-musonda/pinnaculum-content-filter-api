# How initially set up program

- first install all important packages by running:
  `npm install`
- this will install store all necessary packages

# How to run:

` npm start`
this will run using pm2( using clusters so as to improve performance)
